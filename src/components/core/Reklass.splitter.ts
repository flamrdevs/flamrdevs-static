const align = ["ai"] as const;
const justify = ["jc"] as const;
const gap = ["gap", "gapx", "gapy"] as const;
const margin = ["m", "mx", "my", "mt", "mr", "mb", "ml"] as const;
const padding = ["p", "px", "py", "pt", "pr", "pb", "pl"] as const;

export { align, justify, gap, margin, padding };
